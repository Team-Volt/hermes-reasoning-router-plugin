[project]
name = "hermes-reasoning-router-plugin"
version = "0.3.0"
description = "Hermes gateway plugin for automatic Discord reasoning-effort routing."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["PyYAML>=6"]

[tool.pytest.ini_options]
testpaths = ["tests"]
